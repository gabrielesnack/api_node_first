import { User } from '@database/models/User';
import {validate} from "class-validator";
import { CustomError } from '../../exceptions';

class AuthService  {
    constructor() {

    }
    
    public async authAttempt(data: any) {

    }
}

export default new AuthService;